name: moi_geomaticien
description: Application mobile de cours et quiz de géomatique par Novateur221.
publish_to: 'none'
version: 1.4.1+6

environment:
  sdk: '>=3.10.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.5.5
  flutter_local_notifications: ^22.3.0
  workmanager: ^0.9.3
  url_launcher: ^6.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/
    - assets/images/
